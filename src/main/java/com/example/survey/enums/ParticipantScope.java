package com.example.survey.enums;

public enum ParticipantScope {
    ENTIRE_ORGANIZATION,    // All users in the org
    DEPARTMENT,             // Specific department(s)
    TEAM,                   // Specific team(s)
    INDIVIDUAL,             // Specific users
    ROLE_BASED              // All users with a specific role
}
